import React, { useState } from "react";

const App: React.FC = () => {
  return (
    <div>
      <textarea />
      <div>
        <button>Submit</button>
      </div>
    </div>
  );
};

export default App;
